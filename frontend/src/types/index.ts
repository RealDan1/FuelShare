// Centralised TypeScript type definitions
// Feel free to add more shared types/interfaces here to keep DTOs consistent

// Geographical coordinate – pairs of latitude/longitude (WGS-84)
export interface LatLng {
  lat: number;
  lng: number;
}
